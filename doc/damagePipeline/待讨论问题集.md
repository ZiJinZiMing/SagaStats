# 模块化伤害管线：待讨论问题集

> **文档版本**：对齐代码 v4.6 / 模型规范 v4.7
> **最后更新**：2026-04-13
> **代码状态**：v4.6 实现层迭代已全部落地，7 条架构差异见 `全量资料/模块化伤害管线_实现层变更记录_v4.6.md`

---

## 文档说明

| 区域 | 含义 | 行动 |
|------|------|------|
| **Part A: 待讨论（模型级）** | 本质概念问题，需先明确定义再动手 | 深度讨论后写方案 |
| **Part B: 待实施（工具层）** | 方向已定，等待编码落地 | 排期实施 |
| **Part C: 保留观察** | 有倾向但无实证，不紧急 | 后续收集反馈 |
| **Part D: 已解决归档** | 已被迭代消解或已决策完成 | 仅供回溯 |

---

# Part A: 待讨论（模型级）

当前所有待讨论问题都是**本质概念层面**的。工具层和实现层已经稳定。

---

## A1. 攻击上下文的概念定位 🔴 当前焦点

### 问题

在当前模型里，"一个 USTRUCT 被 DamageCondition 消费"这个场景有两种**语义完全不同**的来源，但模型把它们用同一个名字（DamageEffect）包装起来：

| 维度 | DamageEffect（规则产出） | 攻击上下文 |
|---|---|---|
| **本体** | 规则的决策结果 | 事件的原始描述 |
| **存在时机** | 运行时由 Operation 创建 | Pipeline 启动前由调用方填入 |
| **拓扑地位** | DAG 中的**节点**（有生产者 Rule） | DAG 的**外部输入**（无生产者） |
| **因果关系** | 是"果" | 是"因" |
| **R5 规则**适用性 | 参与拓扑排序 | 不参与（没有依赖边可建） |
| **Build 可验证性** | ConsumedEffectType 必须有 producer | 没有 producer 是**正常**的 |

### 为什么"能 work"掩盖了问题

当前实现因为三个巧合刚好能跑：
1. **存储巧合**：两者都是 USTRUCT，可以塞进同一个 `TMap<UScriptStruct*, FInstancedStruct>`
2. **读取巧合**：DamageCondition 只问"DC 有没有这个类型"，不问"谁放进去的"
3. **R3 兜底巧合**：Effect 缺失视为 false 对两者都成立

但三个巧合不等于概念清晰。证据：

- **Build 校验的能力损失**：`DamagePipeline::Build()` 现在**不能**对"ConsumedEffectType 找不到 producer"报错，因为那可能是攻击上下文（合法），也可能是设计者漏配了 Rule（非法）。只能一律容忍。
- **Mermaid 的特判**：导出图表时必须用 `EffectTypeToProducer.Contains(Key)` 特判区分 DC Init 和 DC Final——这个特判是"概念应该有但没有"的地方，代码里偷偷补了。
- **教学的矛盾**：向新开发者解释时，"DamageEffect 是 Rule 的结构化产出" vs "FSekiroAttackContext 也是 DamageEffect"，两句话讲不通。

### 三步检验

**删除测试** — 删掉"攻击上下文"这个独立概念会丢什么？
- 丢掉：设计者对管线输入的清晰心智模型
- 丢掉：Build 时区分"缺失是合法还是 bug"的能力
- 丢掉：Pipeline 的正式输入接口（用于网络同步/回放/单元测试）
- **结论**：丢了本质

**来源追溯** — 这个概念从哪儿长出来的？
- 从问题域本身：动作游戏每次伤害事件必然有"诱因描述"（谁打谁、用什么打、打到哪、什么属性伤害）
- 不是借来的，不是凭空造的
- **结论**：本质复杂度，不可消除

**换表达验证** — 换工具层还存在吗？
- GAS：`FGameplayEffectContextHandle` 承载攻击者/受击者/命中结果（独立于 GameplayEffect 产出）
- GameplayAbilities：`FGameplayAbilityActorInfo` + `FGameplayAbilityActivationInfo` 是 ability 的输入上下文
- 通用战斗系统：任何 DamageCalculator 都有 "Source（输入）" vs "Result（输出）" 的区分
- **结论**：问题域级概念，不是工具层特有

**三项检验都通过 → "攻击上下文"是独立的本质概念，模型必须给它名字。**

### 建议的模型定义

引入两个平级概念，共用 UDamageContext 存储：

**概念 1 - DamageEvent（伤害事件）—— 输入**
> 一次伤害事件的原始描述。由触发攻击的系统（武器/技能/动画通知）在 Pipeline 启动前填入 DC。是 Pipeline 的"参数"。

- 构成：一组 typed struct（如 `FSekiroAttackContext`）
- 特征：无生产者、启动前就位、只读、不参与拓扑排序
- 在 Mermaid 中：DC Init 节点（管线左侧）

**概念 2 - DamageEffect（伤害效果）—— 产出**
> DamageRule 的决策结果。Operation 根据事件和上游 Effect 计算得到，写入 DC。是 Pipeline 的"产物"。

- 构成：一组 typed struct（如 `FGuardEffect`）
- 特征：有唯一生产者 Rule、运行时产生、参与拓扑排序
- 在 Mermaid 中：DAG 节点和 DC Final

**UDamageContext 新定位**：事件数据的容器，装 1 份 DamageEvent + 若干 DamageEffect。

### 定义带来的能力

一旦概念区分，下列能力自然出现：

1. **Build 时校验精确**：
   ```
   for 每个 Condition 的 ConsumedType:
     if 是 DamageEffect: 必须有 producer Rule → 否则 Build 失败
     if 是 DamageEventContext: 必须在 Pipeline 的 RequiredInputs 中 → 否则 Build 失败
   ```
2. **Pipeline 有正式输入接口**：`GetRequiredEventContextTypes()` 列出所有需要调用方填的 struct
3. **Mermaid 的 DC Init 判断从特判变为正式逻辑**
4. **为未来扩展留接口**：网络同步、回放、单元测试

### 待决策

以下几点需要确认后才能写正式方案：

1. **命名**：
   - a) `DamageEvent` + `DamageEffect`（事件 vs 效果，对仗清晰）
   - b) `EventContext` + `DamageEffect`（强调"上下文"）
   - c) 其他？

2. **可选 vs 必需**：某些 Condition 可能消费可选的 EventContext（缺失走 R3 容错），某些必需（缺失就崩）。要区分吗？还是一律 R3 容错？

3. **标记机制**：在 C++/蓝图层面如何识别一个 USTRUCT 是 EventContext 还是 Effect？
   - a) 继承 marker base struct（USTRUCT 继承需小心）
   - b) 约定式命名（`FXxxContext` vs `FXxxEffect`）——软约束
   - c) **Pipeline 资产声明 `TArray<UScriptStruct*> RequiredEventContexts`**（倾向）
     - 不污染 struct 定义本身
     - Build 时可验证
     - 同一个 struct 可在不同 Pipeline 中扮演不同角色
     - Pipeline 的输入签名作为资产的一部分

---

*（A2 已于 2026-04-13 消解，见 Part D11）*

---

# Part B: 待实施（工具层）

## B1. 表现选取阶段（Phase 1.5）🔴 最大的模型-代码差距

**模型规范定义**：Channel、Priority、CombinedPresentation、选取算法。

**代码状态**：
- `UDamageRule::BasePriority` 字段已声明但未使用
- 没有 Channel 概念
- 没有 CombinedPresentation

**依赖**：无，可独立实施。但建议在 A1（攻击上下文定义）完成后再做，因为 CombinedPresentation 可能需要同时引用 EventContext 和 Effect。

---

## B2. Build 时 EffectType 产销校验

**目标**：Build() 阶段报告以下错误：
- Rule 的 Operation 未配置 ProducesEffectType
- Condition 的 ConsumedEffectType 找不到 producer（当 A1 决策完成后）
- 循环依赖

**已实现**：前两项的**前半部分**（ProducesEffectType 校验 + ConsumedEffectType 校验）已在 `DamagePipeline::Build()` 中。

**待实现**：在 A1 决策完成后，区分"合法缺失（EventContext）"和"非法缺失（漏配 Rule）"。

---

## B3. 管线动态组装（Hades 验证）

**目标**：运行时根据玩家选择的 Boon 动态拼装 DamagePipeline。

**代码状态**：`UDamagePipeline::Build()` 已支持运行时调用（`bIsBaked` 会被重置）。

**待验证**：用 Hades 的伤害管线（5-10 个 Boon 组合出 20+ 种变体）作为二号案例，跑通"运行时 Build + Execute"流程。

**依赖**：
- 只狼 MVP 稳定（✓ 已完成）
- A1 攻击上下文模型明确

---

# Part C: 保留观察 🟡

## C1. 双层条件树（Predicate + Condition）的复杂度

**现状**：实现层是 `UDamagePredicate`（AND/OR/Single + bReverse）+ `UDamageCondition`（原子）双层。

**观察点**：
- 双层结构需要 `DamagePredicate_Single` 包装每个原子 Condition，稍显啰嗦
- 模型文档 v4.7 描述的是单层（`UDamageConditionNode` 同时承担 AND/OR 和叶子）
- 实现层选择双层是因为参照了 AIToken 插件的模式，职责更清晰

**决策**：保留双层，不回滚。模型文档和实现文档的差异已在 `实现层变更记录_v4.6.md` 中记录。

---

*（C2 已于 2026-04-13 消解，见 Part D12）*

---

## C3. UDamageContext 命名

**现状**：容器类叫 `UDamageContext`，内部只存 `TMap<UScriptStruct*, FInstancedStruct> DamageEffects`。

**观察点**：
- Context 这个名字在 A1 决策后可能需要重新审视
- 如果 A1 决定引入 DamageEvent 概念，UDamageContext 作为"装 Event + Effects"的容器，名字可能改为 `UDamageData` 或保持 `UDamageContext` 都说得通

**决策**：等 A1 完成后一并决定。

---

# Part D: 已解决归档 🟢

## D1. ✅ DPU → DamageRule 术语重构（v4.6）

**决策**：代码完成 v4.5 → v4.6 的术语全面重构。DPU/Fact/Logic/PipelineAsset → DamageRule/DamageEffect/DamageOperation/DamagePipeline。CoreRedirects 已完整覆盖。

## D2. ✅ Execute 签名改为 OutEffect 直传

**决策**：`Execute(DC, OutEffect&)`，框架预创建 OutEffect 传入，Operation 只填字段，框架负责写 DC。R2 自洽性恢复（Operation 不需要知道自己的身份标识）。

## D3. ✅ Predicate/Condition 双层分离

**决策**：参照 AIToken 模式，职责分离。Predicate 负责组合逻辑（AND/OR + bReverse），Condition 负责原子检查。

## D4. ✅ UScriptStruct* 作为 DC 键

**决策**：类型即 key，编译时类型安全，DamageRule:Effect 1:1 关系。

## D5. ✅ 移除 EventContext/标量 API/ContextCheck

**决策**：`SetBool/GetFloat/SetInt` 等散碎接口移除，攻击上下文改用 typed struct + SetEffectByType。`UDamageCondition_ContextCheck` 删除，替换为具体的 Condition 子类（如 `UDamageCondition_IsLightning`、`UDamageCondition_IsInAir`）。

## D6. ✅ 移除 RuleName / PipelineName / Description 冗余字段

**决策**：DamageRule 和 DamagePipeline 的身份直接使用资产/对象的 `GetFName()`/`GetName()`，不再存冗余字段。`FRuleExecutionEntry::RuleName` 保留作为日志接口。

## D7. ✅ DamagePipeline Graph Editor（只读 DAG 可视化）

**决策**：`FDamagePipelineAssetEditor` 提供双面板编辑器（Details + Graph），Build 按钮在 Toolbar。Graph 是 Build 后的只读可视化，参照 AIGraph 模式，节点从左到右按拓扑层级自动布局（与 Mermaid `graph LR` 一致）。

## D8. ✅ 蓝图 CustomThunk API 独立为 Library

**决策**：`UDamageContextLibrary` 作为 `UBlueprintFunctionLibrary`，提供 `SetEffect`/`GetEffect` 通配结构体接口。**不能**在 UDamageContext 成员函数上直接使用 CustomThunk + CustomStructureParam，会因缺少 K2Node 配套导致 Stack 解析错乱崩溃。

## D9. ✅ FactMethodRegistry 删除

**决策**：v4.6 实现层选择"子类即域方法"后，全局注册表成为偶然复杂度，整套删除。

## D12. ✅ 子类即域方法 vs MethodName 下拉（消解 C2）

**原担忧**：子类方式在 Hades 级复杂管线下可能"爆炸"。

**消解理由**（2026-04-13）：

**担忧本身是错的**：
- N 个域方法 = N 个下拉项 = N 个子类，数量线性相等
- 子类数量爆炸是把"类的数量"和"文件数量"混淆了
- 每个子类是域方法的壳子（几行代码），不是传统 OOP 的"一个类"

**子类方式实际上比 MethodName 下拉更优**：

1. **能配置实例参数**（MethodName 做不到）
   ```cpp
   UCLASS(meta=(DisplayName="HasZeusBonusLevel"))
   class UDamageCondition_HasZeusBonusLevel : public UDamageCondition
   {
       UPROPERTY(EditAnywhere) int32 MinLevel = 3;       // 实例参数
       UPROPERTY(EditAnywhere) EZeusSkill RequiredSkill; // 多个参数自由组合
   };
   ```
   MethodName 下拉做不到实例参数化——要么硬编码成 N 个方法，要么引入通用参数机制（JSON/反射，偶然复杂度爆炸）。

2. **类型安全**
   - 子类的 `ConsumedEffectType` 在类级别硬编码（`HideInDetailPanel`）
   - Evaluate 内直接 cast 到具体类型，编译器检查
   - 重命名子类编译器报错；重命名 MethodName 字符串运行时静默失效

3. **蓝图可扩展性**
   - 蓝图用户可通过创建 UDamageCondition 子类（蓝图）扩展新域方法
   - MethodName 下拉需要全局注册表（像之前删除的 FactMethodRegistry）

4. **发现性**
   - Class Defaults 面板直接看到参数
   - MethodName 需要文档说明方法含义和前提

**结论**：子类即域方法不是"实现层的妥协"，而是"实现层优于模型层"的改进。这是 `实现层变更记录_v4.6.md` 记录的 7 条差异中应该反哺回模型文档的部分。

## D11. ✅ DamageEffect 缺失语义的精度（消解 A2）

**原问题**：R3 规定"Effect 缺失视为 false"，数值型字段上"缺失"和"零/默认"无法区分，是否需要更精细的约定？

**消解理由**（2026-04-13）：

R3 只是"默认行为约定"，不是"强制规则"。Condition 子类有完整的表达能力：

1. **类型级缺失可精确感知**：
   - `TMap<UScriptStruct*, FInstancedStruct>` 的 key 是类型本身，不是内存布局
   - 即使 FStructA 和 FStructB 字段完全相同，它们有不同的 UScriptStruct*，DC 查找可正确区分
   - `GetEffect<T>()` 返回 nullptr 或 `ConsumedEffect.IsValid() == false` 都是明确信号
   - 蓝图 CustomThunk `execGetEffect` 走 Valid/NotValid 执行引脚，双重保险

2. **Condition 作者保有语义决定权**：
   ```cpp
   bool UDamageCondition_HasZeusBonus::Evaluate_Implementation(
       const UDamageContext* Context, const FInstancedStruct& ConsumedEffect) const
   {
       const FZeusBoonContext* Zeus = ConsumedEffect.GetPtr<FZeusBoonContext>();
       if (!Zeus) return false; // 或 true，由 Condition 作者决定
       return Zeus->Level >= 3;
   }
   ```

3. **字段级缺失是 Operation 的约定责任**：
   - 如果 Operation 作者需要区分"值为零"和"未设置"，让结构体字段带 `bHasValue` 标志位或用 `TOptional<T>`
   - 这是 Operation 内部设计问题，不是管线模型问题

**结论**：问题域层面不存在 A2，只有 Condition 作者的实现选择。模型规范不需要额外约定，只需说明"R3 是默认约定，Condition 子类可自定义"。

## D10. ✅ 独立 Fact Key 概念删除

**决策**：经三步检验确认为偶然复杂度。现以 UScriptStruct* 为 DC 索引键。

## D13. ✅ DamageOperation BP 通配返回值不可行（接受标准范式）

**问题**：DamageOperation 的 BlueprintNativeEvent `Execute(Context, OutEffect&)` 在 Blueprint 实现时，用户必须用 `Set Instanced Struct Value(OutEffect, MyStruct)` 把具体结构体塞进 OutEffect。能否消掉这个中间节点，让 BP 直接"返回"具体结构体由框架自动包装？

**调研结论**（2026-04-13）：**标准引擎机制无法做到**。

**根本原因**：

`CustomStructureParam` + `CustomThunk` 是引擎支持通配结构体引脚的唯一机制，但**只处理"被调用的函数"**，不处理"被重写的事件函数"：

| 场景 | 节点类 | 是否处理 CustomStructureParam |
|---|---|---|
| 调用静态库函数（如 `MakeInstancedStruct`） | `UK2Node_CallFunction` | ✅ 通过 `FCustomStructureParamHelper::UpdateCustomStructurePins` |
| 重写 BlueprintNativeEvent 函数体 | `UK2Node_Event::CreatePinsForFunctionEntryExit` | ❌ 不调用 Helper，忽略 meta |

即使在 BlueprintNativeEvent 参数上打 `CustomStructureParam` 也无效——引擎根本不读这个 meta。

**引擎为什么这么设计**：

引擎刻意分离了两种职责：
- **被调用函数**（CallFunction）：支持动态类型绑定，因为调用点在 BP 图上，拓扑已知
- **被重写函数**（Event）：拒绝动态类型绑定，因为 override 的语义契约要求子类遵守父类签名

允许 BlueprintNativeEvent 参数类型被子类"改变"会破坏 LSP（Liskov 替换原则）——父类承诺调用 `Execute(Context, OutEffect)`，但子类声称它接受一个具体 struct，C++ 侧调用父类签名时不知道怎么构造参数。

`UK2Node_GetDataTableRow` 实现"通配返回值"的方式是：**它不是 override，是 call**。通配 K2Node 只在调用侧出现，不在被重写侧出现。

**唯一替代路径**：写自定义 K2Node（参考 `UK2Node_GetDataTableRow` 约 600 行编辑器代码），在 Blueprint 编译期注入 `Set Instanced Struct Value` 调用。

**决策**：保持现状，接受 "Make X → Set Instanced Struct Value" 标准范式。

**理由**：
- 这是 UE 任何写 `FInstancedStruct&` 输出参数的函数都用的模式，和引擎一致
- 只狼 13 个 + Hades 30-50 个 DamageRule 规模下，不值得为消减 1 个节点投入 600 行编辑器代码 + 后续维护
- K2Node API 在引擎主版本间可能变动，维护成本持续

**调研涉及的关键文件**（避免后续重复调研）：
- `Engine/Source/Runtime/Engine/Classes/Kismet/BlueprintInstancedStructLibrary.h` — Make/Set/Get InstancedStruct UFUNCTION 声明
- `Engine/Source/Runtime/Engine/Private/BlueprintInstancedStructLibrary.cpp` — 三个 CustomThunk `DEFINE_FUNCTION`
- `Engine/Source/Editor/BlueprintGraph/Private/K2Node_CallFunction.cpp:93-176` — `FCustomStructureParamHelper`
- `Engine/Source/Editor/BlueprintGraph/Private/K2Node_Event.cpp:347-370` — 事件节点 pin 创建（不经过 Helper）
- `Engine/Source/Editor/BlueprintGraph/Private/K2Node_GetDataTableRow.cpp` — 自定义 K2Node 通配返回值范本

---

# 附录：v4.6 代码实现状态

## ✅ 已完成

| 模块 | 文件 |
|---|---|
| DC 统一存储 | `UDamageContext` —— `TMap<TObjectPtr<UScriptStruct>, FInstancedStruct>` + UPROPERTY |
| 条件树双层 | `UDamagePredicate` + `UDamageCondition` |
| Operation 基类 | `UDamageOperationBase`（Execute OutEffect 直传） |
| DamageRule DataAsset | `UDamageRule`（无冗余字段，GetFName 为身份） |
| Pipeline 自洽执行 | `UDamagePipeline`（Build + Execute + Mermaid 导出 + SortedRules 持久化） |
| Sekiro MVP 验证 | 13 个 DamageRule（Mixup/Guard/Hurt/Death/Collapse/Poison/Lightning 等） |
| 蓝图 CustomThunk API | `UDamageContextLibrary::SetEffect/GetEffect` |
| CoreRedirects | `DefaultSagaStats.ini` 约 60 条重定向 |
| Graph Editor | `FDamagePipelineAssetEditor` + 只读 DAG 可视化 |
| 测试 Actor | `ADamagePipelineTestActor`（5 个只狼场景，1-5 键触发） |

## 🔴 待完成

| 项目 | 对应 | 阻塞 |
|---|---|---|
| 攻击上下文模型定义 | **A1** | 无（当前焦点） |
| 表现选取阶段 | B1 | 建议等 A1 |
| Build 时合法性校验区分 | B2 | 等 A1 |
| Hades 动态管线验证 | B3 | 等 A1 |

---

**相关文档**：
- `全量资料/模块化伤害管线_模型规范文档_v4_7.md` — 模型规范 v4.7
- `全量资料/模块化伤害管线_实现层架构文档.md` — 实现层架构 v4.7
- `全量资料/模块化伤害管线_DamageEffect模型设计.md` — DamageEffect 模型设计 v3
- `全量资料/模块化伤害管线_实现层变更记录_v4.6.md` — v4.6 实现层相对 v4.7 模型的 7 条差异
- `Doc/damagePipeline/架构设计.md` — 代码架构文档
- `Doc/damagePipeline/快速入门.md` — 新成员入门
- `Doc/damagePipeline/术语速查表.md` — 术语对照
